sudo: required

language: python

services:
  - docker

before_install:
  - pwd
  - docker run -v $HOME:/work /bin/bash smintz/nomadgen-builder -c "cd /work; pip install setuptools && make deps && make"

script:
  - make
